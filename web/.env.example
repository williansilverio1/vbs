# Optional: override the enquiry form Google Apps Script Web App URL.
# If unset, the app uses the same default URL as the static site (root index.html).
# Copy to .env only when you need a different deployment (e.g. staging).
#
# VITE_GOOGLE_SHEET_WEBAPP_URL=https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec
